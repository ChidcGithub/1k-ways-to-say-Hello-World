---
id: 0001
language: python
paradigm: procedural
output: stdout
platform: cli
era: modern
tags: []
notes: "The most canonical Hello World in Python 3."
author: ChidcGithub
date: 2026-07-20
---

# #0001 · Python Classic

## Description

The classic Python one-liner using `print()`. The most canonical Hello World in Python 3 — a single call to the built-in `print` function, which writes the given string to standard output followed by a newline.

## Source Code

`main.py`:

```python
print("Hello, World!")
```

## Run

```bash
cd hello-worlds/python/0001-classic
python main.py
```

## Categorization

| Dimension | Value | Reason |
|-----------|-------|--------|
| language | `python` | Python 3, using the built-in `print` function |
| paradigm | `procedural` | A single sequential statement; no classes, closures, or data flow |
| output | `stdout` | `print()` writes to standard output by default |
| platform | `cli` | Runs from a terminal/command-line interpreter |
| era | `modern` | Python 3's `print()` function syntax dates to 2008 (Python 3.0), placing it in the 2001–2020 modern era |
| tags | `[]` | No special technique worth indexing — it is the baseline reference implementation |

## Reference

- [Python 3 `print()` documentation](https://docs.python.org/3/library/functions.html#print)
